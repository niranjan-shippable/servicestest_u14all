language: none

services:
  - sqlite 

build:
  ci:
    - node --version

integrations:
  notifications:
    - integrationName: asdfa
      recipients:
        - niranjan8192@gmail.com      
      type: email
      on_success: always
      on_failure: always
      on_start: always
