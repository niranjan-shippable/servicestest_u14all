language: none

services:
  - sqlite 

build:
  ci:
    - node --version

integrations:
  notifications:
    - integrationName: email-niranjan-shippable  
      type: email
      on_success: always
      on_failure: always
      on_start: always
      on_pull_request: always
